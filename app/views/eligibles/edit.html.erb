<div class="d-flex justify-content-center">
<%= form_with(model: @eligible, url: eligible_path(@eligible), method: :patch, html: { style: "min-width: 30%;" }) do |form| %>
  <% if @eligible.errors.any? %>
    <div class="mt-3 alert alert-danger alert-dismissable fade show" role="alert">
      <% @eligible.errors.full_messages.each do |msg| %>
      <div><%= msg %></div>
      <% end %>
    </div>
  <% end %>
  <section class="card shadow my-5 pt-4" style="background-color: rgba(114, 108, 248, 0.3);">
    <div class="card-body">
      <h3 class="card-title text-white mb-4"><%= Section.find_by(id: @eligible.section_id).name.capitalize %> Criteria Update</h3>
      <div class="form-floating mb-3">
        <%= form.text_field :age, class: "form-control", placeholder: "6" %>
        <%= form.label :age, "Minimum Age" %>
      </div>
      <div class="form-floating mb-3">
        <%= form.text_field :grade, class: "form-control", placeholder: "40" %>
        <%= form.label :grade, "Minimum percentage" %>
      </div>
      <div class="d-flex text-center gap-3">
        <%= form.submit "Update", class: "button__student w-100" %>
        <%= link_to "Back", section_path, class: "button__student w-100" %>
      </div>
    </div>
  </section>
<% end %>
</div>